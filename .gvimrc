set guifont=Monaco:h13
set background=dark
colorscheme gruvbox
let g:gruvbox_contrast_dark='hard'
let g:airline_theme='gruvbox'

" Changes highliting. Must come after colorscheme
:highlight! def link SpecialKey Normal

" Typwriter scrolling
set scrolloff=999
